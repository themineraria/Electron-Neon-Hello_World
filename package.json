{
  "name": "electron-neon-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "install": "electron-build-env neon build ../neon --release",
    "version": "electron-build-env neon build ../neon --release"
  },
  "devDependencies": {
    "electron": "^8.0.0",
    "electron-build-env": "^0.2.0",
    "eslint": "^6.8.0",
    "neon-cli": "^0.3.3"
  },
  "dependencies": {},
  "eslintConfig": {
    "rules": {
      "import/no-unresolved": "off"
    }
  }
}
